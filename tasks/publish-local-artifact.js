var mvn;


module.exports = function(grunt) {
    grunt.registerTask('publish-local-artifact','Find prepared overlay and publish to local maven repository', function(){
    		// check if overlay exists
    		// run maven on it
    		//  done or say what's wrong

    		//*imporove overlay to take version from 'version'
    });
};